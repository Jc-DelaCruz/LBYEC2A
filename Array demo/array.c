#include <stdio.h>

int main(){
    // // Declaration
    // int numbers[4];

    // //access or assign values
    // numbers[0] =10;
    // numbers[1] =20;
    // numbers[2] =30;
    // numbers[3] =40;

    // //display
    // printf("The first number is %d.", numbers[0]);
    // printf("\nThe second number is %d.", numbers[1]);
    // printf("\nThe third number is %d.", numbers[2]);
    // printf("\nThe fourth number is %d.", numbers[3]);

    
    return 0;
}